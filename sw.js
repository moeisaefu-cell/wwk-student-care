// Service Worker เบื้องต้นสำหรับรองรับ PWA
self.addEventListener('install', (e) => {
  // ติดตั้งเสร็จสมบูรณ์
});

self.addEventListener('fetch', (e) => {
  // จัดการดึงข้อมูลแคชเพื่อความรวดเร็วเมื่อใช้งานแอป
});
